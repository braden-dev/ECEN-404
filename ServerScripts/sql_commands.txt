SQL Commands:
1) set auto_inc to zero:
> alter table images auto_increment=0;
2) show a column in a table
> SELECT id FROM images;
3) delete the data in a table:
> delete from images;
4) connect to database
> connect [db_name];
>> ex: connect ecen404;